import 'package:flutter/material.dart';


class OptionalStockWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('optional'),
    );
  }
}
